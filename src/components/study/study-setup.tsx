"use client";

import { useEffect, useState } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { BookOpen, Play, Shuffle } from "lucide-react";
import { Button } from "@/components/ui/button";
import { db } from "@/features/storage/db";
import type { QuestionSet } from "@/types/question";
import type { QuizMode, QuizOptions } from "@/types/quiz";

export function StudySetup() {
  const router = useRouter(); const [sets, setSets] = useState<QuestionSet[]>([]); const [setId, setSetId] = useState(""); const [mode, setMode] = useState<QuizMode>("practice"); const [order, setOrder] = useState<"sequential" | "random">("sequential"); const [start, setStart] = useState(""); const [end, setEnd] = useState(""); const [count, setCount] = useState("");
  useEffect(() => { void db.questionSets.orderBy("updatedAt").reverse().toArray().then((items) => { setSets(items); if (items[0]) setSetId(items[0].id); }); }, []);
  const selected = sets.find((item) => item.id === setId); const startQuiz = () => { const options: QuizOptions = { questionSetId: setId, mode, order, startNumber: start ? Number(start) : undefined, endNumber: end ? Number(end) : undefined, questionCount: count ? Number(count) : undefined }; sessionStorage.setItem("quizforge-quiz-options", JSON.stringify(options)); sessionStorage.setItem("quizforge-current-set-id", setId); router.push("/quiz"); };
  if (!sets.length) return <main className="mx-auto max-w-xl px-6 py-24 text-center"><BookOpen className="mx-auto size-9 text-primary" /><h1 className="mt-5 text-2xl font-bold">Chưa có bộ đề nào</h1><p className="mt-2 text-muted-foreground">Import, kiểm tra và lưu bộ câu hỏi trước khi bắt đầu học.</p><Button className="mt-6" render={<Link href="/import" />} nativeButton={false}>Nhập bộ câu hỏi</Button></main>;
  return <main className="mx-auto min-h-dvh max-w-3xl px-4 py-10 sm:px-6"><Link href="/" className="text-sm font-semibold text-primary">← QuizForge</Link><p className="mt-8 text-sm font-semibold uppercase tracking-[0.16em] text-primary">Thiết lập phiên học</p><h1 className="mt-2 text-3xl font-bold">Bạn muốn học như thế nào?</h1><section className="mt-9 space-y-7"><label className="block"><span className="text-sm font-semibold">Bộ đề</span><select value={setId} onChange={(event) => setSetId(event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-input bg-card px-3 outline-none focus:border-primary"><option value="" disabled>Chọn bộ đề</option>{sets.map((item) => <option value={item.id} key={item.id}>{item.name} · {item.questions.length} câu</option>)}</select></label><div className="grid gap-3 sm:grid-cols-2"><button type="button" onClick={() => setMode("practice")} className={`border-l-4 p-4 text-left ${mode === "practice" ? "border-primary bg-muted" : "border-border"}`}><p className="font-semibold">Luyện tập</p><p className="mt-1 text-sm text-muted-foreground">Biết đúng/sai ngay sau khi chọn.</p></button><button type="button" onClick={() => setMode("exam")} className={`border-l-4 p-4 text-left ${mode === "exam" ? "border-primary bg-muted" : "border-border"}`}><p className="font-semibold">Thi thử</p><p className="mt-1 text-sm text-muted-foreground">Nộp bài rồi mới xem kết quả.</p></button></div><div><p className="text-sm font-semibold">Thứ tự câu hỏi</p><div className="mt-2 flex gap-3"><Button variant={order === "sequential" ? "default" : "outline"} onClick={() => setOrder("sequential")}><BookOpen /> Theo thứ tự</Button><Button variant={order === "random" ? "default" : "outline"} onClick={() => setOrder("random")}><Shuffle /> Ngẫu nhiên</Button></div></div><div className="grid gap-4 sm:grid-cols-3"><label className="text-sm font-semibold">Từ câu<input type="number" min="1" value={start} onChange={(event) => setStart(event.target.value)} placeholder="1" className="mt-2 h-11 w-full rounded-lg border border-input bg-card px-3 font-normal outline-none focus:border-primary" /></label><label className="text-sm font-semibold">Đến câu<input type="number" min="1" value={end} onChange={(event) => setEnd(event.target.value)} placeholder={String(selected?.questions.length ?? "")} className="mt-2 h-11 w-full rounded-lg border border-input bg-card px-3 font-normal outline-none focus:border-primary" /></label><label className="text-sm font-semibold">Số câu tối đa<input type="number" min="1" value={count} onChange={(event) => setCount(event.target.value)} placeholder="Tất cả" className="mt-2 h-11 w-full rounded-lg border border-input bg-card px-3 font-normal outline-none focus:border-primary" /></label></div><p className="text-sm text-muted-foreground">Dùng “Từ câu / Đến câu” để học theo chương nếu bạn đã đánh số câu liên tiếp theo chương trong tài liệu.</p><Button size="lg" disabled={!setId} onClick={startQuiz}><Play /> Bắt đầu {selected ? `${selected.questions.length} câu` : ""}</Button></section></main>;
}
